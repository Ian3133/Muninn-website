# Muninn website agent guidance

Before editing, read `../AGENT_HANDOFF.md` and check the current Git status.
Use `.ua/knowledge-graph.json` for implementation navigation once generated.
For publishing or data-contract work, also read
`../.agents/understanding/index.md` and inspect the backend graph.

Generated files under `public/Current_news/` are inputs to the UI, not the
frontend's implementation source. Verify graph claims against current React
components, styles, tests, and build configuration.
